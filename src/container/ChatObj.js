import { connect } from 'react-redux'
import ChatBar from '../component/ChatBar'

const mapStateToProps = state => ({

})

const mapDispatchToProps = dispatch => ({
})

const CharObej = connect(
  mapStateToProps,
  mapDispatchToProps,
)(ChatBar)

export default CharObej
